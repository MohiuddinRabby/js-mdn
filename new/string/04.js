// Write a JavaScript function to extract a specified number of characters from a string.
function extractString(string, length) {
  console.log(string.trim().substring(0, length));
}
extractString(" okay extracted", 5);
